{
  "buildCommand": "npm run build --workspace=@meccha/client",
  "outputDirectory": "apps/client/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
